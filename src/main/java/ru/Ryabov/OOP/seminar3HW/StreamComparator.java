package ru.Ryabov.OOP.seminar3HW;

import java.util.Comparator;

public class StreamComparator implements Comparator<Stream>{

    @Override
    public int compare(Stream stream1, Stream stream2) {
        int resOfComp = stream1.getGroups().co

        return 0;
    }
}
